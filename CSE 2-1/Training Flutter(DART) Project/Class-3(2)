class Person{
//   bool can_do=true;
//   bool? can_do;
//   final bool can_do;
  final int? id;
  final String? name;
  final int? age;
  
  Person({required this.name,this.age,this.id});
}

void main(){
  Person p1 = Person(name: "person name", age: 12, id:10223);
  
//   print({p1.name, p1.age});
//   print([p1.name, p1.age]);
  print([p1.id, p1.name, p1.age]);
}
